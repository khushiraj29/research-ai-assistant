[pytest]
# This file lets you run `pytest` from inside the backend/ directory.
# When run from the project root, the root-level pytest.ini is used instead.
testpaths = tests
addopts = -v --tb=short
pythonpath = ..
